{
  "name": "trustfabric-firebase",
  "version": "1.0.0",
  "description": "Firebase functions for TrustFabric project",
  "main": "functions/src/index.ts",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase-admin": "^10.0.0",
    "firebase-functions": "^3.20.0"
  },
  "devDependencies": {
    "typescript": "^4.5.0",
    "eslint": "^7.32.0"
  },
  "engines": {
    "node": "14.x"
  },
  "private": true
}